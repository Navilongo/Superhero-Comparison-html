var svgWidth = 900;
var svgHeight = 500;

var margin = {
    top:
    right:
    bottom:
    left:
}

var jsonpath = ("resources/")

d3.json(jsonpath).then(function(data) {
    console.log(data);
  });
  